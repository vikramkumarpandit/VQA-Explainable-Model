[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "gunicorn backend.app:app --bind 0.0.0.0:$PORT --timeout 600"

[build.env]
PYTHON_VERSION = "3.10"
